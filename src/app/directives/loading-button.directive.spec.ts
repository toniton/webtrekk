import { LoadingButtonDirective } from './loading-button.directive';

describe('LoadingButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingButtonDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
